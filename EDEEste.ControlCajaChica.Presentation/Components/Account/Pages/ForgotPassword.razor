@page "/Account/ForgotPassword"

<PageTitle>Olvidé mi contraseña</PageTitle>

<h1>Olvidé mi contraseña</h1>

<div class="row">
    <div class="col-md-5">
        @if (cuentaNoExiste)
        {
            <div class="alert alert-warning">
                Esta cuenta no existe. <a href="Account/Register">Ir a registrarse</a>.
            </div>
        }

        <p class="text-muted">
            Escriba su usuario. Un Administrador debe aprobar la solicitud antes de
            que pueda definir una contraseña nueva.
        </p>

        <EditForm Model="Input" FormName="forgot-password" OnValidSubmit="OnValidSubmitAsync" method="post">
            <DataAnnotationsValidator />
            <ValidationSummary class="text-danger" role="alert" />

            <div class="form-floating mb-3">
                <InputText @bind-Value="Input.Usuario" id="Input.Usuario" class="form-control"
                           autocomplete="username" aria-required="true" placeholder="nombre.apellido" />
                <label for="Input.Usuario" class="form-label">Usuario</label>
                <ValidationMessage For="() => Input.Usuario" class="text-danger" />
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">Solicitar restablecimiento</button>
        </EditForm>
    </div>
</div>
